# Sample CLI session (from the package root, after pip install -e .)

postking
# Welcome, then:
#   postking ui
#   postking new
#   postking doctor
#   postking --help

postking --help

postking version
# postking 0.1.0

postking new --difficulty steward --seed 1
postking move e2e4
postking status

postking doctor
postking status --json

# UI (this computer only)
# postking ui
# Open http://127.0.0.1:8844/
